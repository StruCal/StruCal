import { AccelerationFormatPipe } from './acceleration-format.pipe';

describe('AccelerationFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AccelerationFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
