import { Canvas3dDirective } from './canvas3d.directive';

describe('Canvas3dDirective', () => {
  it('should create an instance', () => {
    const directive = new Canvas3dDirective();
    expect(directive).toBeTruthy();
  });
});
